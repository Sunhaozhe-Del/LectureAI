services:
  - type: web
    name: lectureai-web
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: LECTUREAI_MODEL
        value: gpt-5-mini
